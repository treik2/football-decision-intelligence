# CI is disabled — set to manual trigger only to avoid costs during development.
# To re-enable, change `on:` back to `push` / `pull_request`.
name: CI

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: CI disabled
        run: echo "CI is currently disabled. Enable by updating the on: trigger."
